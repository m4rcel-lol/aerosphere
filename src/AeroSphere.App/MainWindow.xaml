<Window
    x:Class="AeroSphere.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AeroSphere"
    Width="1400"
    Height="900">

    <Grid Background="{ThemeResource AppShellBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="68" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:NavigationView
            Grid.Column="0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            PaneDisplayMode="LeftMinimal"
            SelectionFollowsFocus="Enabled">
            <controls:NavigationView.MenuItems>
                <controls:NavigationViewItem Content="Dashboard" Icon="Home" />
                <controls:NavigationViewItem Content="Activity" Icon="Clock" />
                <controls:NavigationViewItem Content="Files" Icon="Document" />
                <controls:NavigationViewItem Content="Apps" Icon="AllApps" />
                <controls:NavigationViewItem Content="System" Icon="Admin" />
                <controls:NavigationViewItem Content="Shortcuts" Icon="Pin" />
            </controls:NavigationView.MenuItems>
            <controls:NavigationView.FooterMenuItems>
                <controls:NavigationViewItem Content="Settings" Icon="Setting" />
            </controls:NavigationView.FooterMenuItems>
        </controls:NavigationView>

        <Grid Grid.Column="1" Padding="20,16,20,16" RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <AutoSuggestBox
                    Grid.Column="0"
                    PlaceholderText="Search apps, files, folders, settings, commands (Ctrl+K)"
                    MinWidth="360"
                    QueryIcon="Find" />

                <TextBlock
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center"
                    Text="☀ 71°F Clear"
                    Style="{ThemeResource BodyStrongTextBlockStyle}" />

                <TextBlock
                    Grid.Column="2"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center"
                    Text="Mon, Apr 20 • 9:41 AM"
                    Style="{ThemeResource BodyTextBlockStyle}" />

                <Button Grid.Column="3" Margin="12,0,0,0" Content="Refresh" />
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:UniformGrid Columns="3" HorizontalAlignment="Stretch" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Weather Hub" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Current + hourly + 7-day forecast with offline cache fallback." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Calendar / Planner" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Today focus, events, reminders, and compact month view." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Smart Daily Insights" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Disk, battery, weather alerts, and useful non-spam suggestions." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Recent Files" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Filter by type, quick open, pin, copy path, open containing folder." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Recent Folders" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Best-effort folder history from supported Windows sources." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Recent Apps" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Launch, pin, sort recent vs most used, transparent source quality." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
